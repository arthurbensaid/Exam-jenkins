apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cast-db.fullname" . }}-test-connection"
  labels:
    {{- include "cast-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cast-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
